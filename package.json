
{
  "name": "otpsim-web",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx esbuild index.tsx --bundle --outfile=dist/bundle.js --format=esm --jsx=automatic --minify --loader:.tsx=tsx --loader:.ts=ts --platform=browser --define:process.env.API_KEY=\\\"$API_KEY\\\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "esbuild": "^0.24.0"
  }
}
