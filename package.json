
{
  "name": "otpsim-web",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npx esbuild index.tsx --bundle --outfile=bundle.js --format=esm --jsx=automatic --minify --platform=browser --define:process.env.API_KEY=\\\"$API_KEY\\\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@google/genai": "^1.0.0",
    "esbuild": "^0.24.0"
  }
}
