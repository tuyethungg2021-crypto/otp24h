
{
  import http from 'node:http';
  "name": "otpsim-web",
  "version": "1.0.0",
  "description": "Nền tảng thuê SIM OTP",
  "main": "server.js",
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview"
},
  "devDependencies": {
  "vite": "^5.0.0",
  "@vitejs/plugin-react": "^4.2.0"
},
  "engines": {
    "node": ">=18.0.0"
  }
}
